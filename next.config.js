/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/promethea-solutions-lp', 
    images: {
      unoptimized: true,
    },
    
  };
  
  module.exports = nextConfig;